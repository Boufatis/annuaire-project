<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pnr"
    android:versionCode="1"
    android:versionName="1.0" >
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher" 
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.pnr.MainActivity"
            android:label="PNR - Parc Normandie Maine" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Parc"
            android:label="Listes des exploitations" />
         <activity
            android:name=".TabExploitation"
            android:label="Exploitation"/>
         <activity
            android:name=".uneExploitation"
            android:label="Exploitation"
            android:theme="@android:style/Theme.NoTitleBar" />
         <activity
            android:name=".ContactExploitation"
            android:label="Exploitation"
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>

</manifest>
